# Regenerate: flutter test tool/render_splash_test.dart   (the mascot PNGs)
#             dart run flutter_native_splash:create
#
# Coral with the mascot's face, the first frame of the animated intro
# (lib/shared/widgets/launch_intro.dart). The intro starts from exactly this
# picture — same colour, same face, same size — so the handoff is invisible.
flutter_native_splash:
  color: "#F16743"
  image: assets/branding/splash_mascot.png
  android_12:
    color: "#F16743"
    image: assets/branding/splash_mascot_android12.png
  android: true
  ios: false # no iOS project yet
  web: false
  fullscreen: false
